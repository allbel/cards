const initState: any = {}

export const authReducer = (state = initState, action: any): any => {
    switch (action.type) {
        // case 'TYPE':
        //     return {...state}
        default:
            return state
    }
}